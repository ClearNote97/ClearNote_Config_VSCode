# Normalización de fin de línea: LF en todo el texto.
# CRÍTICO: los scripts .sh DEBEN quedar en LF, o se rompen al ejecutarse en
# Linux/macOS/Git Bash ("$'\r': command not found" / shebang inválido).
* text=auto eol=lf

*.sh   text eol=lf
*.json text eol=lf
*.txt  text eol=lf
*.md   text eol=lf
